"""Shared pytest fixtures for austin-power tests.

No shared fixtures are needed yet; this file exists so pytest treats
``tests/`` as a rootdir-relative package and future tasks have a place
to add cross-module fixtures.
"""
